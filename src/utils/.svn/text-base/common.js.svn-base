/**
 * Created by zhengxiuming on 2017/11/18.
 */
export const objToForm = (v)=> {
  var str = '';
  for (let o in v) {
    str += o + '=' + v[o] + '&';
  }
  str = str.substring(0, str.length - 1);
  return str;
};

