========布局========
1、Layout组件包含了Nav导航组件和底部Tab-bar组件。
2、Nav导航组件默认有两个主题：白色与黑色。
<- - - - - ->
使用：页面如果有Tab-bar使用Layout组件即可，如果没有单纯使用Nav即可。
参数：bgBlack=>({true} or {false});title=>("导航文本");


========调试/发布=======
===《开发》===
1、npm run build:dll #第一次npm run start时需运行此命令，使开发时编译更快
2、npm run start 启动本地开发

===《编译》===
> npm run build
将会打包至dist/{version}目录（也就是当前版本）

> npm run build:new
将会打包至dist/{version增加1}目录 （当前版本加1）

===《代码检测》===
> npm run lint

